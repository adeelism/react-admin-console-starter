# Production build for the hosted demo: no backend, so serve everything from the
# in-browser MSW mocks. Point VITE_API_BASE_URL at a real API and unset the mock
# flags to run against a live backend instead.
VITE_API_BASE_URL=/api
VITE_ENABLE_MOCKS=true
VITE_MOCK_MODULES=users,audit-log
