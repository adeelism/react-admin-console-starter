# Base URL for the API the HTTP client talks to.
VITE_API_BASE_URL=/api

# Turn MSW request mocking on and choose which modules are mocked.
# Comma-separated module names, or "all". Empty/unset = no mocking (real API).
VITE_ENABLE_MOCKS=true
VITE_MOCK_MODULES=users,audit-log
